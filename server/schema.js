export const schemaStatements = [
  `CREATE TABLE IF NOT EXISTS products (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    handle VARCHAR(255) NOT NULL,
    ref VARCHAR(100) NOT NULL UNIQUE,
    name VARCHAR(255) NOT NULL,
    category VARCHAR(255),
    description TEXT,
    sold_by_weight BOOLEAN NOT NULL DEFAULT FALSE,
    supplier VARCHAR(255),
    purchase_cost NUMERIC(10, 2),
    sale_price NUMERIC(10, 2) NOT NULL DEFAULT 0,
    barcode VARCHAR(255),
    available_for_sale BOOLEAN NOT NULL DEFAULT TRUE,
    raw_payload JSONB,
    created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP
  )`,
  `CREATE TABLE IF NOT EXISTS app_settings (
    setting_key VARCHAR(100) PRIMARY KEY,
    setting_value JSONB NOT NULL,
    updated_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP
  )`,
  `CREATE TABLE IF NOT EXISTS employees (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    role VARCHAR(255) NOT NULL,
    hourly_rate NUMERIC(10, 2) NOT NULL DEFAULT 0,
    tax_id VARCHAR(50) NOT NULL,
    social_security_number VARCHAR(100) NOT NULL,
    login_code VARCHAR(60) UNIQUE,
    pin_hash VARCHAR(64) NOT NULL,
    mobile_access_enabled BOOLEAN NOT NULL DEFAULT TRUE,
    created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP
  )`,
  `CREATE TABLE IF NOT EXISTS employee_shifts (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    employee_id INTEGER NOT NULL REFERENCES employees(id) ON DELETE CASCADE,
    started_at TIMESTAMPTZ NOT NULL,
    ended_at TIMESTAMPTZ,
    started_verification_at TIMESTAMPTZ NOT NULL,
    ended_verification_at TIMESTAMPTZ,
    verification_method VARCHAR(50) NOT NULL DEFAULT 'pin',
    ended_verification_method VARCHAR(50),
    device_id VARCHAR(100),
    created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP
  )`,
  `CREATE TABLE IF NOT EXISTS employee_mobile_sessions (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    employee_id INTEGER NOT NULL REFERENCES employees(id) ON DELETE CASCADE,
    token_hash VARCHAR(64) NOT NULL UNIQUE,
    expires_at TIMESTAMPTZ NOT NULL,
    created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP
  )`,
  `CREATE TABLE IF NOT EXISTS clients (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    tax_id VARCHAR(100) NOT NULL,
    address VARCHAR(255),
    postal_code VARCHAR(20),
    city VARCHAR(120),
    email VARCHAR(255),
    phone VARCHAR(50),
    created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP
  )`,
  `CREATE TABLE IF NOT EXISTS invoices (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    invoice_number VARCHAR(100) NOT NULL UNIQUE,
    issue_date DATE NOT NULL,
    due_date DATE NOT NULL,
    client_id INTEGER REFERENCES clients(id) ON DELETE SET NULL,
    client_name VARCHAR(255) NOT NULL,
    tax_id VARCHAR(100),
    client_address VARCHAR(255),
    client_postal_code VARCHAR(20),
    client_city VARCHAR(120),
    client_email VARCHAR(255),
    client_phone VARCHAR(50),
    payment_by_transfer BOOLEAN NOT NULL DEFAULT FALSE,
    status VARCHAR(20) NOT NULL DEFAULT 'pendiente',
    notes TEXT,
    vat_rate NUMERIC(6, 2) NOT NULL DEFAULT 21,
    subtotal NUMERIC(10, 2) NOT NULL DEFAULT 0,
    vat_amount NUMERIC(10, 2) NOT NULL DEFAULT 0,
    total NUMERIC(10, 2) NOT NULL DEFAULT 0,
    created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP
  )`,
  `CREATE TABLE IF NOT EXISTS invoice_items (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    invoice_id INTEGER NOT NULL REFERENCES invoices(id) ON DELETE CASCADE,
    description VARCHAR(255) NOT NULL,
    quantity NUMERIC(10, 2) NOT NULL DEFAULT 0,
    unit_price NUMERIC(10, 2) NOT NULL DEFAULT 0,
    line_total NUMERIC(10, 2) NOT NULL DEFAULT 0,
    created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP
  )`,
  `CREATE TABLE IF NOT EXISTS daily_orders (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    order_date DATE NOT NULL UNIQUE,
    status VARCHAR(20) NOT NULL DEFAULT 'open',
    csv_filename VARCHAR(255) NOT NULL,
    csv_path VARCHAR(500) NOT NULL,
    total_items INTEGER NOT NULL DEFAULT 0,
    created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP
  )`,
  `CREATE TABLE IF NOT EXISTS daily_order_items (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    daily_order_id INTEGER NOT NULL REFERENCES daily_orders(id) ON DELETE CASCADE,
    product_id INTEGER NOT NULL REFERENCES products(id),
    variant_name VARCHAR(255),
    quantity_ordered INTEGER NOT NULL,
    purchase_cost NUMERIC(10, 2) NOT NULL DEFAULT 0,
    created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP
  )`,
  `CREATE TABLE IF NOT EXISTS daily_closings (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    daily_order_id INTEGER NOT NULL UNIQUE REFERENCES daily_orders(id) ON DELETE CASCADE,
    total_units_sold INTEGER NOT NULL DEFAULT 0,
    total_sales_amount NUMERIC(10, 2) NOT NULL DEFAULT 0,
    closed_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP
  )`,
  `CREATE TABLE IF NOT EXISTS daily_closing_items (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    daily_closing_id INTEGER NOT NULL REFERENCES daily_closings(id) ON DELETE CASCADE,
    daily_order_item_id INTEGER NOT NULL REFERENCES daily_order_items(id) ON DELETE CASCADE,
    remaining_quantity INTEGER NOT NULL,
    sold_quantity INTEGER NOT NULL,
    unit_sale_price NUMERIC(10, 2) NOT NULL DEFAULT 0,
    sales_amount NUMERIC(10, 2) NOT NULL DEFAULT 0,
    created_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP
  )`,
]
